## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem being solved. Link an issue if there is one. -->

## Checks

- [ ] `make check` passes (hadolint, shellcheck, unit tests)
- [ ] `make up && make smoke` passes, or CI will run it
- [ ] New behaviour has a test
- [ ] Nothing new runs as root or needs a writable root filesystem
- [ ] No secrets, real hostnames, or real credentials

## Claims

<!-- Does this add a statement to the README about how the container behaves?
     If so, which test proves it? -->
